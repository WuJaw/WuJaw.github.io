# 2026-05-27

## 启用 Jekyll 博客

为 WuJaw.github.io 完成 Jekyll 配置：

- 创建 `_config.yml`（kramdown + permalink: /posts/:title/）
- 创建 `_layouts/default.html`（通用布局，含完整 CSS 样式）
- 创建 `_layouts/post.html`（文章布局，显示标题+日期+正文）
- 改造 `index.html` 为 Jekyll 模板（Liquid 自动列出 _posts 文章）
- 改造 `about.html` 为 Jekyll 模板
- 迁移旧文章 `posts/蓝牙wifi版本床垫指示灯.html` → `_posts/2025-01-01-蓝牙wifi版本床垫指示灯.md`
- 创建示例文章 `_posts/2026-05-27-hello-jekyll.md`
- 创建流程文档 `_posts/2026-05-27-jekyll-build.md`（配置流程、工具链、推送踩坑记录）
- 推送踩坑：Git Bash 无 TTY 时需 `git -c credential.helper=wincred push`

**写文章方式**：在 `_posts/` 下新建 `YYYY-MM-DD-标题.md`，开头加 Front Matter（layout: post, title, date），推送到 GitHub 自动编译。

## 博客外观升级 — lsy22 风格

参照 blog.lsy22.com 升级 `_layouts/default.html`：
- 引入 CSS 变量设计令牌（亮/暗双模式），配色白底 → 暗底 `#111315`
- 字体层级 10 级（micro→caption→meta→small→ui→body→h3→h2→h1→title）
- 添加主题切换按钮（🌙/☀️），localStorage 持久化，默认跟随系统 prefers-color-scheme
- 导航栏 active 状态高亮
- 微调 `about.html` class 名适配新样式
- Jekyll 框架不变，只改 CSS 层
