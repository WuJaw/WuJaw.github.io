# 2026-07-29 工作日志

## 数分微站出厂测试软件操作手册更新 V1.0 → V1.1

- `python/launcher.py`（2026-07-22 修改）Excel 写入逻辑从"同编号覆盖+删除空行"变更为"始终追加新行"
- `assets/launcher.py`（2026-07-01 修改）仍为旧版覆盖逻辑，未同步
- 手册更新内容：
  - 文件重命名：`V1.0.md` → `V1.1.md`
  - Front matter title: V1.0 → V1.1
  - 步骤八增加说明：每次测试结果始终以新行追加到 Excel 末尾，不覆盖同编号旧数据
  - 版本记录增加 V1.1 条目

## 同步 SVN 与项目中的同名 Python 文件

对比 `D:\SVN2\vs_studio\python\main\` 与项目 `python/` 目录中的同名 .py 文件：

- `launcher.py`：SVN `Microstation/launcher.py` 与项目 `python/launcher.py` 已一致（07-22 最新版），但 `assets/launcher.py` 仍为旧版 → 已用 `python/launcher.py` 覆盖更新
- `submit_work_hours.py`：两处已完全一致，无需操作
- `wch_isp_gui_enhanced.py`：SVN 版本（06-12 16:37）比项目（06-12 15:34）新，新增 `get_app_dir()` 函数兼容 pyinstaller 打包 → 已从 SVN 复制更新到项目
