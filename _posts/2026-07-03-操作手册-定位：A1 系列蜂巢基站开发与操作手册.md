---
layout: post
title: "定位：A1 系列蜂巢基站开发与操作手册"
date: 2026-07-03
category: 操作手册
---

A1 系列蜂巢基站基于 Linux 系统运行，开发时通过 VS Code Remote SSH 连接远程编译部署，本文汇总从环境搭建到日常运维的完整操作流程。

---

## 1 开发环境准备

### 1.1 安装 VS Code

- 下载安装 [Visual Studio Code](https://code.visualstudio.com/)

### 1.2 安装 Remote SSH 插件

在 VS Code 扩展市场中搜索并安装 **Remote - SSH** 插件。

---

## 2 连接基站

1. 打开 VS Code，按 `F1` 或 `Ctrl+Shift+P` 打开命令面板
2. 输入 `Remote-SSH: Connect to Host`
3. 输入基站 IP 地址，连接成功后打开远程目录

---

## 3 登录

SSH 连接后使用以下凭据登录：

```shell
用户名：root
密码：develop
```

---

## 4 编译流程

进入项目目录后，执行以下命令：

```bash
# 清理上次编译产物
make clean

# 重新编译
make
```

编译完成后，生成的可执行文件为 `./iot_app`。

---

## 5 部署流程

### 5.1 停止服务

```bash
systemctl stop iot_app.service
```

### 5.2 替换可执行文件

```bash
cp ./iot_app /usr/local/bin/iot_service/
```

### 5.3 启动服务

```bash
systemctl start iot_app.service
```

### 5.4 查看服务状态

```bash
systemctl status iot_app.service
```

---

## 6 访问与配置

### 6.1 Web 管理界面

基站管理页面地址（示例）：

```
http://10.10.5.213:8090
```

---

## 7 日志查看

### 7.1 日志目录

基站有两套服务，各自有日志：

**定位服务日志：**

```shell
cd /usr/local/bin/rtls_service/log/debug
```

**物联网服务日志：**

```shell
cd /usr/local/bin/iot_service/log/debug
```

### 7.2 实时查看日志

```bash
tail -f ./debug.log
```

### 7.3 日志过滤

按关键词过滤日志内容，例如过滤特定设备数据：

```bash
tail -f ./debug.log | grep "91 e0 03 16"
```

---

## 8 文件打包与下载

### 8.1 安装 zip 工具

```bash
apt install zip
```

### 8.2 压缩目录

```bash
zip -r ./2.iot_service20260506.zip ./2.iot_service/
```

### 8.3 下载到本地

在 VS Code 文件浏览器中，右键点击目标文件，选择 **下载** 即可将文件保存到本地。

---

## 9 Git 操作

### 9.1 克隆仓库

```bash
git clone git@10.10.5.66:root/iot_integrated_gateway.git
```

### 9.2 Git 全局配置

**禁用行尾符自动转换**（防止 Windows/Linux 换行符冲突）：

```bash
git config --global core.autocrlf false
```

**允许处理长路径**（Windows 下克隆深目录仓库时需要）：

```bash
git config --global core.longpaths true
```

---

## 10 附录：常用命令速查

| 操作 | 命令 |
|------|------|
| 登录 | `root` / `develop` |
| 停止服务 | `systemctl stop iot_app.service` |
| 启动服务 | `systemctl start iot_app.service` |
| 查看状态 | `systemctl status iot_app.service` |
| 编译 | `make clean && make` |
| 定位日志 | `tail -f /usr/local/bin/rtls_service/log/debug/debug.log` |
| 物联网日志 | `tail -f /usr/local/bin/iot_service/log/debug/debug.log` |
| 过滤日志 | `tail -f debug.log \| grep "关键词"` |
| 打包 | `zip -r ./xxx.zip ./目录/` |
| 克隆仓库 | `git clone git@10.10.5.66:root/iot_integrated_gateway.git` |
| Git 配置 | `git config --global core.autocrlf false` |
