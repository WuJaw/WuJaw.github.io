---
layout: post
title: "工时自动填报脚本"
date: 2026-06-08
category: python
---

## 1 背景

公司工时系统是一个 Vue 3 + Element Plus 的 Web 应用，每天要手动登录填报。工作内容已经写在周报 Excel 里，重复填写效率低下，于是写了 Python 脚本自动完成提交。

源码放在 [python/submit_work_hours.py](https://github.com/WuJaw/WuJaw.github.io/blob/master/python/submit_work_hours.py)，附带[项目列表导出脚本](https://github.com/WuJaw/WuJaw.github.io/blob/master/python/export_projects.py)。

## 2 接口分析

### 2.1 抓包方式

工时系统在内网，外部工具无法直接访问。通过浏览器 F12 Console 注入拦截器来捕获请求：

```javascript
(function(){
  var origOpen = XMLHttpRequest.prototype.open;
  var origSend = XMLHttpRequest.prototype.send;
  XMLHttpRequest.prototype.open = function(){
    var xhr = this;
    xhr.addEventListener('readystatechange', function(){
      if(xhr.readyState === 4){
        console.log('[POST] ' + xhr._u + ' => ' + xhr.status);
        console.log('  Response: ' + xhr.responseText.substring(0, 500));
      }
    });
    origOpen.apply(this, arguments);
    xhr._u = arguments[1];
  };
  XMLHttpRequest.prototype.send = function(body){
    console.log('Body: ' + body);
    return origSend.apply(this, arguments);
  };
  console.log('Interceptor started');
})();
```

### 2.2 关键接口

| 接口 | 说明 | 请求体 |
|------|------|--------|
| `POST /api/dailyReport/add` | 新建日报 | 数组格式 |
| `POST /api/dailyReport/update` | 更新日报 | 对象格式，需带 `id` |
| `POST /api/dailyReport/list` | 查询日报列表 | 分页参数 |

关键差异：`add` 接口接收**数组**，`update` 接口接收**对象**且必须带 `id` 字段，否则服务端 SQL 会因缺少 `id` 限定查出大量重复记录，报 `Expected one result (or null) to be returned by selectOne()` 错误。

### 2.3 认证方式

认证通过 Cookie 完成：

```
TMS_SESSION_ID=xxxx; rememberMe=xxxx; token=111
```

其中 `token=111` 是用户 ID，脚本首次运行手动输入后，会自动保存到 `config.json`，后续无需再填。

## 3 周报 Excel 结构

周报文件结构如下（Sheet 名：`周报`）：

| 星期 | 日期 | 工作状态 | 项目编码 | 工作内容 | 备注 |
|------|--------|---------|---------|---------|------|
| 周一 | 2026-01-05 | 办公 | RD240012 | 对接需求定义 | |
| 周二 | 2026-01-06 | 办公 | RD240009 | 软著材料编写 | |
| ... | ... | 法定假 | | | 休息 |

每周之间有"本周工作重点"和"下周工作计划"分隔行，脚本解析时会自动跳过。

工作状态分类：

- **工作日**：`办公`、`出差`、`外勤` → 提交 8 小时
- **休息日**：`法定假`、`周六`、`周日`、`年假`、`病假`、`调休` 等 → 跳过不提交

## 4 脚本实现

### 4.1 文件选择

脚本启动时弹窗选择 Excel 文件（依赖 `tkinter`，Windows 自带）：

```python
import tkinter as tk
from tkinter import filedialog

root = tk.Tk()
root.withdraw()
path = filedialog.askopenfilename(
    title='选择周报 Excel 文件',
    filetypes=[('Excel 文件', '*.xlsx *.xls')],
    initialdir=r'D:\SVN2\docx\周报'
)
```

若 `tkinter` 不可用，则回退到命令行输入路径。

### 4.2 核心流程

```
读 config.json → 验证 Cookie → 弹窗选 Excel
    → 解析日期/状态/项目/内容
    → 查询已有记录（区分 add / update）
    → 逐条提交（间隔 0.3s）
```

关键设计：

1. **Cookie 持久化** — 首次手动输入后保存到 `config.json`，自动复用
2. **项目编码匹配** — 读 Excel 的"项目编码"列，为空则使用默认值 `RD240009`
3. **新增 vs 更新** — 提交前查询该日期是否已有记录，有则 `update`，无则 `add`
4. **休息日跳过** — 法定假、周末等状态在解析阶段直接过滤

### 4.3 使用方式

```powershell
# 安装依赖
pip install pandas openpyxl requests

# 运行（首次会弹窗要求输入 Cookie）
python submit_work_hours.py
```

Cookie 获取：浏览器打开工时系统 → F12 → Console → 执行 `copy(document.cookie)` → 粘贴到脚本提示处。

## 5 踩坑记录

### 5.1 update 接口 selectOne 报错

用 `/api/dailyReport/update` 创建新记录（不带 `id`），服务端返回：

```json
{"code":"202","msg":"程序异常，稍后重试","info":"Expected one result (or null) to be returned by selectOne(), but found: 12497"}
```

解决：区分 `add`（新建，不带 `id`）和 `update`（更新，带 `id`）两个接口。

### 5.2 add 请求体是数组

通过抓包发现 `add` 接口的请求体是 JSON **数组**，每个元素代表一条日报；`update` 是单个**对象**。第一次用对象格式调 `add` 被服务端静默忽略。

### 5.3 F12 Network 看不到 POST

部分 Vue 项目在页面跳转时清空 Network 记录。解决：用 Console 注入 XHR 拦截器，在请求发出时直接 `console.log` 打印，不受 Network 面板影响。

### 5.4 Excel 编码问题

Windows 下 Python 默认编码非 UTF-8，读含有中文的 Excel 或 Cookie 时需注意。脚本中显式使用 `encoding='utf-8'` 读写文件。

## 6 扩展：项目列表导出

附带 `export_projects.py`，可从工时系统导出所有项目信息到 Excel：

```powershell
python export_projects.py
```

导出内容：项目编码、名称、状态、任务列表、负责人等，方便填报时参考项目编码。

源码：[python/export_projects.py](https://github.com/WuJaw/WuJaw.github.io/blob/master/python/export_projects.py)

## 7 总结

| 项目 | 说明 |
|------|------|
| 依赖 | `pandas`, `openpyxl`, `requests` |
| 认证 | Cookie 持久化到 `config.json` |
| 新建 | `POST /api/dailyReport/add`（数组） |
| 更新 | `POST /api/dailyReport/update`（对象，带 id） |
| 数据源 | 周报 Excel → 弹窗选择文件 |
| 自动化 | 休息日跳过、已提交检测、请求间隔 0.3s |
| 源码 | [submit_work_hours.py](https://github.com/WuJaw/WuJaw.github.io/blob/master/python/submit_work_hours.py) |
