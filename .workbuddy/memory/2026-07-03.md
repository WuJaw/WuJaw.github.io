# 2026-07-03

## 隐藏文章功能（暗号解锁）

在首页搜索框中实现了隐藏文章的暗号解锁机制：
- 文章 front matter 加 `secret: true` 即标记为隐藏
- 在搜索框输入暗号（默认 `open-sesame`）即可切换显示/隐藏
- 解锁状态存 localStorage，刷新后保持
- 首页分类卡片计数自动排除未解锁的隐藏文章
- 侧边栏目录也同步隐藏/显示

修改文件：
- `index.html`：secret-post 类标记 + djb2 hash 暗号检测 + 计数修正
- `_layouts/default.html`：侧边栏 secret-post CSS + localStorage 检查脚本

修改暗号方法：浏览器控制台执行 `djb2('新暗号')`，把返回值填入 index.html 的 `SECRET_HASH`。

## 废弃 hidden_categories，统一为 secret_categories

删除了 `_config.yml` 中的 `hidden_categories`，把"博客1"移入 `secret_categories`。`index.html` 和 `default.html` 中对应的 `unless hidden_categories` 判断已移除。新增 `secret-cat` 类支持整分类标记为私密。

## 博客分类文章合并

将"博客"分类下 8 篇 + "python"分类下 2 篇 Jekyll 相关文章合并为 1 篇 `2026-07-03-博客-Jekyll 博客搭建全记录.md`，共 12 个章节：目录结构、配置、设计风格、图片处理、自动添加 Front Matter、标题自动编号、语法高亮、搜索功能与暗号解锁、侧边目录、Liquid 逃逸、Fork 部署、工具链。删除了原始的 10 个 md 文件。

文章已按实际项目状态更新：
- 目录结构补充了 git_reset.bat / git_rollback.bat / tools/ 子目录内容
- _config.yml 的 secret_categories 更新为实际的 6 个分类
- 修正 add_fm.bat / auto_number.bat / autocommit.bat 代码与实际文件一致
- 搜索章节补充了暗号解锁机制说明
- 工具链添加了 git_reset / git_rollback
- 发布流程修正 autocommit.bat 已含 push

## 更新 README.md

按实际项目状态重写 README：
- 目录结构：反映真实的 python/（11 个脚本）、tools/（4 个文件）、根目录（9 个 .bat）
- 修正宽度为 900px（非 720px）
- 补充 secret_categories 私密分类机制说明
- 文章分类更新为实际分类（操作手册/基础知识/编程/工具/github/博客，共 27 篇）
- 去掉不存在的 deploy.bat 和 change/ 目录
- 工作流改为按实际步骤：web.bat → add_fm.bat → auto_number.bat → autocommit.bat

## 更新 git_push.bat 为主线

用户实际使用 `git_push.bat`（一键部署）而非逐步骤操作。更新了：
- README.md：`git_push.bat` 标注为"一键部署"，`autocommit.bat` 改为"单独 git add + commit + push"
- Jekyll 博客搭建全记录.md：目录结构、12.2 脚本表格、12.3 发布流程全部反映 git_push.bat 为主线
