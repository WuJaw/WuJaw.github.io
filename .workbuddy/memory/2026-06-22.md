## 2026-06-22

### 在线工具页"打开"按钮样式改造

文章：`2026-06-02-工具-在线工具.md`（`.tool-list / .tool-item / .btn`）

把 `_layouts/default.html` 里的 `.btn` 从「黑底白字实心矩形 + opacity 反馈」改为：

- 圆角胶囊形（`border-radius: 999px`）
- 白底 + 深色边框 + 深色文字的线框样式
- 文字后追加箭头 `→`（外链用 `.btn--external` 替换为 `↗`）
- hover：反色成实心黑底 + 文字上移 1px + 浅阴影 + 箭头右移 2px
- active：归位 + 阴影变小，模拟按压
- 同步在 `2026-06-02-工具-在线工具.md` 给 hexed、draw.io 两条外链加上 `btn--external` 与 `target="_blank" rel="noopener"`

### 微调：去掉黑色元素

反馈"按钮不要黑色"。重写 `.btn` 为浅色版：

- 默认：白底 + `var(--line)` 浅灰边框 + `var(--muted)` 文字 + 灰色箭头
- hover：边框/文字/箭头全部加深到 `var(--ink)`，箭头右移 2px（**不再反色成黑底**）
- active：背景换成 `var(--soft)`，给一点点按压反馈

`.btn--external` 修饰类继续保留（外链显示 `↗`），颜色与普通按钮一致，不再单独加深。
