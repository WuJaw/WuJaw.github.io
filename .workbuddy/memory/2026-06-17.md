# 2026-06-17

## 分类归档页实现

- 新增 `_layouts/category.html` — 分类归档页 layout，显示分类名、文章数、文章列表（按时间倒序）
- 新增 `categories/` 目录，创建 8 个分类页面：ADC验证、python、博客、定位、工具、操作手册、物联网、送检
  - 每个页面 permalink 格式：`/categories/分类名/`
- 首页 `index.html`：分类卡片标题 `.cat-name` 由 `<span>` 改为 `<a>` 链接，指向对应分类归档页
- 左侧站点导航 (`default.html`)：分类名从 `javascript:void(0)` 改为实际分类页链接
- `default.html` CSS：`.cat-name` 增加 `text-decoration: none` 和 hover 效果

## 注意

分类页用 Jekyll 原生 `site.categories[cat_name]` 过滤，无需插件，GitHub Pages 直接支持。
